<UserControl x:Class="MosqueMateV2.CustomUserControls.CarouselUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Main Content -->
        <Grid x:Name="SlideContainer">
            <ItemsControl 
                ItemsSource="{Binding Slides, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
                
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="400" Height="300">
                            <Image Source="{Binding ImagePath}" Stretch="UniformToFill" />
                            <StackPanel VerticalAlignment="Bottom" Background="#80000000" >
                                <TextBlock Text="{Binding Title}" FontSize="16" Foreground="White" />
                                <TextBlock Text="{Binding Description}" FontSize="12" Foreground="White" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
 
        <!-- Navigation Buttons -->
        <Button Content="❮" Width="40"  Height="40" HorizontalAlignment="Left" VerticalAlignment="Center" Click="PrevButton_Click" />
        <Button Content="❯" Width="40" Height="40"  HorizontalAlignment="Right" VerticalAlignment="Center" Click="NextButton_Click" />
    </Grid>

</UserControl>
