<Window x:Class="MosqueMateV2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xag="clr-namespace:XamlAnimatedGif;assembly=XamlAnimatedGif"
        xmlns:local="clr-namespace:MosqueMateV2"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="800"
        MinWidth="600" MinHeight="400" 
        WindowStartupLocation="CenterScreen" 
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown" ThemeMode="System">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="243*"/>
            <RowDefinition Height="341*"/>
        </Grid.RowDefinitions>
        <Border CornerRadius="10" Margin="10,10,10,0" BorderBrush="Azure" BorderThickness="0.1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid>
                <Label x:Name="welcomeLBL" Content="....." FontSize="12"  Padding="10 15 0 0" Margin="0,0,390,50"/>
                <Label x:Name="hijiriDateLBL" Content="....." FontWeight="Bold" FontSize="15" Padding="10 0 0 0" Margin="0,44,390,6"/>
            </Grid>
        </Border>
    </Grid>
</Window>
